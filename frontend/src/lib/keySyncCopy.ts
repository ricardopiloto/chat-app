import { t } from "../i18n";

/** Shared E2EE key-sync copy (inventory C8 / 074). */
export function keySyncMsg(): string {
  return t("channel.keySync");
}

export function keySyncShort(): string {
  return t("channel.keySyncShort");
}
