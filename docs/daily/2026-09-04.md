# Daily — 2026-09-04

## Speckit implement

### [007-shell-create-plus](../../specs/007-shell-create-plus/)

Concluído `/speckit-implement` (T001–T023).

**Entrega**
- «+» no fundo do rail de servidores → criar servidor com custódia; bootstrap texto `geral` + voz `mesa` (chave + cena default)
- «+» nos labels **Texto** / **Voz e vídeo** (só dono); tipo implícito; removidos botões textuais «Criar servidor» / «Criar canal»
- Delete canal: 409 `last_channel_of_type` (mínimo 1 texto + 1 voz)
- Canal de voz: UI de uma cena; mantém **Editar cena** na activa; multi-cena → backlog G10
- Tema Mesa: CSS do rail sticky-plus + secções; build Vite OK

**Validação**
- `cargo test` — contract 31 + integration 2 OK
- `npx tsc --noEmit` / `npm run build` OK

**Artefactos**
- Spec/plan/tasks: `specs/007-shell-create-plus/`
- CHANGELOG: entrada sob `[Unreleased]`

### [008-shell-chrome-members](../../specs/008-shell-chrome-members/)

Concluído `/speckit-implement` (T001–T020).

**Entrega**
- Botões de acção `.btn` em pílula (`--radius-pill` / 999px)
- Composer de texto a largura total do pane (removido `max-width` 74ch)
- Modo palco: rail permanece; coluna de canais em faixa + «mostrar canais» (expande sem sair do palco)
- Painel **Membros** à direita (cabeçalho texto/voz); troca de servidor mantém painel e refresca via `GET .../members`

**Validação**
- `npx tsc --noEmit` OK
- Manual: [quickstart.md](../../specs/008-shell-chrome-members/quickstart.md)

**Artefactos**
- Spec/plan/tasks: `specs/008-shell-chrome-members/`
- CHANGELOG: entrada sob `[Unreleased]`

### [009-chat-media-embeds](../../specs/009-chat-media-embeds/)

Concluído `/speckit-implement` (T001–T023).

**Entrega**
- Anexos imagem/GIF (≤10, ≤8 MiB) cifrados no cliente (server key); blobs opacos em `ATTACHMENTS_DIR`
- `POST/GET` attachments + `attachment_ids` nas mensagens; unfurl lazy `POST /api/unfurl` (SSRF guards)
- Composer: attach/preview/remove; histórico com mídia + cartões de link

**Validação**
- `cargo test --test contract` — 36 OK (incl. attachments/unfurl)
- `npx tsc --noEmit` OK

**Artefactos**
- Spec/plan/tasks: `specs/009-chat-media-embeds/`
- CHANGELOG: entrada sob `[Unreleased]`

### [010-media-paste-webp](../../specs/010-media-paste-webp/)

Concluído `/speckit-implement` (T001–T020).

**Entrega**
- Colar imagem em todo o painel do canal de texto → anexos pendentes; texto+imagem → pending + draft
- Colagens estáticas → WebP (0.82); GIF animado mantém GIF
- Limite por anexo **5 MiB** (FE + BE + body limit + contract test)

**Validação**
- `cargo test` — contract + integration OK
- `npx tsc --noEmit` OK

**Artefactos**
- Spec/plan/tasks: `specs/010-media-paste-webp/`
- CHANGELOG: `[Unreleased]` Added/Changed

### [011-text-message-delete](../../specs/011-text-message-delete/)

Concluído `/speckit-implement` (T001–T021).

**Entrega**
- `DELETE /api/channels/{id}/messages/{messageId}` — autor / criador do canal / dono do servidor
- Hard delete + limpeza de anexos em disco; WS `message.deleted`
- UI: «Apagar» no hover/foco + `confirm`; sync remoto sem reload

**Validação**
- `cargo test` — 39 contract + 2 integration OK
- `npx tsc --noEmit` OK

**Artefactos**
- Spec/plan/tasks: `specs/011-text-message-delete/`
- CHANGELOG: `[Unreleased]` Added

### [012-shell-iconography-typography](../../specs/012-shell-iconography-typography/)

Concluído `/speckit-implement` (T001–T035).

**Entrega**
- Sistema de ícones SVG (`components/icons/`) — chamada, E2EE, sidebar, topbar
- Topbar funcional: pesquisa (mensagens decifradas nos servidores do utilizador), notificações WS, definições (tema + sessão)
- Tipografia: `--font-mono` em chave/handles/convite; pesos de heading h1–h3 vs h4–h6
- Backend intocado

**Validação**
- `npx tsc --noEmit` OK

**Artefactos**
- Spec/plan/tasks: `specs/012-shell-iconography-typography/`
- CHANGELOG: `[Unreleased]` Added/Changed

### [013-topbar-scene-ux](../../specs/013-topbar-scene-ux/)

Concluído `/speckit-implement` (T001–T020).

**Entrega**
- Toggle de tema na topbar (`IconSun`/`IconMoon` = estado actual); removidos Definições/`SettingsPanel`
- Menu de conta no chip (handle só leitura + Terminar sessão + diálogo de confirmação)
- Pesquisa: ícone → campo inline na topbar (sem modal Dialog)
- Editor de cena full-pane (`1fr` + ~296px) alinhado ao Protótipo v2

**Validação**
- `npx tsc --noEmit` OK

**Artefactos**
- Spec/plan/tasks: `specs/013-topbar-scene-ux/`
- CHANGELOG: `[Unreleased]` Added/Changed/Removed

### [014-search-channel-scope](../../specs/014-search-channel-scope/)

Concluído `/speckit-implement` (T001–T017).

**Entrega**
- Ctrl+F / Cmd+F no shell; em canal de texto substitui o campo por `#nome `
- Sintaxe `#canal termo` (só texto); sem `#` → todos os canais de texto acessíveis
- Empty states: Canal não encontrado / Só canais de texto / Sem resultados
- Placeholder `Pesquisar… ou #canal termo`

**Validação**
- `npx tsc --noEmit` OK

**Artefactos**
- Spec/plan/tasks: `specs/014-search-channel-scope/`
- CHANGELOG: `[Unreleased]` Added/Changed

### [016-plus-create-modals](../../specs/016-plus-create-modals/)

Concluído `/speckit-implement` (T001–T018).

**Entrega**
- `Dialog` Portal montado sob `.app` + `data-theme` espelhado em `<html>`; tema claro/escuro ao vivo com modal aberto
- Superfície `.dialog*` e inputs partilhados alinhados a tokens Mesa (`--elev`, `--input-bg`, scrim, Escape para fechar)
- Erros de criação visíveis dentro dos modais «+» (canal/servidor); chrome de custódia (`.key-display` / `.check-line`) com contraste em ambos os temas

**Validação**
- `npx tsc --noEmit` OK
- Sem alterações de backend nesta feature (diff `backend/` pré-existente de 011)
- Manual: [quickstart.md](../../specs/016-plus-create-modals/quickstart.md)

**Artefactos**
- Spec/plan/tasks: `specs/016-plus-create-modals/`
- CHANGELOG: `[Unreleased]` Changed/Fixed

### [015-camera-background-blur](../../specs/015-camera-background-blur/)

Concluído `/speckit-implement` (T001–T020).

**Entrega**
- Blur de fundo na câmara ao vivo (leve / forte / sem), no feed publicado (sala + gravação); não aplica a «Vídeo de teste»
- Controlo partido no botão Câmara (toggle vs seta/menu); seta com forma distinta quando o blur está ligado
- Preferência `mesa.cameraBlur` neste dispositivo; MediaPipe servido na mesma origem (`/mediapipe/`)
- Primeiro frame: não publica nítido com leve/forte já escolhido; falha do efeito pára o vídeo (áudio continua)

**Validação**
- `npx tsc --noEmit` OK
- Sem alterações de backend
- Manual de duas contas / câmara real: [quickstart.md](../../specs/015-camera-background-blur/quickstart.md) (não executado aqui — precisa de webcam e segundo participante)

**Artefactos**
- Spec/plan/tasks: `specs/015-camera-background-blur/`
- CHANGELOG: `[Unreleased]` Added/Changed

### [017-search-jump-highlight](../../specs/017-search-jump-highlight/)

Concluído `/speckit-implement` (T001–T017).

**Entrega**
- Clicar num hit da pesquisa navega para `/channels/{id}?…&message={id}` e recolhe o painel
- Canal de texto centra a mensagem (`scrollIntoView` center) e aplica `.msg-highlight` ~3 s (novo salto substitui; scroll/clique não limpam)
- Se o id não estiver na página actual: até 5 fetches `?before=`; se falhar, toast «Mensagem não encontrada» sem destaque fantasma
- Sintaxe 014 (`#canal`, Ctrl+F) intocada

**Validação**
- `npx tsc --noEmit` OK; ESLint nos ficheiros tocados OK
- Sem alterações de backend nesta feature
- Quickstart browser §1–§4: não executado aqui (sem ferramentas de browser nesta sessão)

**Artefactos**
- Spec/plan/tasks: `specs/017-search-jump-highlight/`
- CHANGELOG: `[Unreleased]` Added

### [018-scene-camera-count](../../specs/018-scene-camera-count/)

Concluído `/speckit-implement` (T001–T023).

**Entrega**
- BE: `validate_layout` aceita `slot_count` 2–8 sem acoplar a `layout_key`; provision voz 2–8; contract tests mestre+6 / faixa+3 / rejeita 1 e 9
- FE: `layoutGeometry(key,n)` + controlo N no editor; picker ao reduzir com slots ocupados; labels dinâmicos (Faixa N-up, Painel)
- Palco ao vivo só após Guardar; `docs/operar-instancia.md` actualizado (2–8)

**Validação**
- `npx tsc --noEmit` OK
- `cargo test --test contract` — 42 OK (incl. `scene_slot_count`)
- Quickstart browser §1–§5: não executado aqui (sem browser nesta sessão)

**Artefactos**
- Spec/plan/tasks: `specs/018-scene-camera-count/`
- CHANGELOG: `[Unreleased]` Added + Changed

### [019-members-invite-icons](../../specs/019-members-invite-icons/)

Concluído `/speckit-implement` (T001–T012).

**Entrega**
- Cabeçalho de canal (texto e voz): «Membros» passou a ícone de grupo; o botão fica seleccionado (`--press`) com o painel aberto
- Cabeçalho do servidor: ícone pessoa+ à direita do nome, **só para o dono**; botão textual «Convite» no fundo da lista removido
- Diálogo de convite e painel de membros (título «Membros») inalterados

**Validação**
- `npx tsc --noEmit` OK; ESLint nos ficheiros tocados OK
- Sem alterações de backend nesta feature
- Quickstart browser §1–§4: não executado aqui (sem ferramentas de browser nesta sessão)

**Artefactos**
- Spec/plan/tasks: `specs/019-members-invite-icons/`
- CHANGELOG: `[Unreleased]` Changed

### [020-call-control-icons](../../specs/020-call-control-icons/)

Concluído `/speckit-implement` (T001–T013).

**Entrega**
- Microfone / Câmara: só ícone + `title` ≡ `aria-label` (ligado/desligado)
- Split câmara: contentor Discord-like (borda partilhada + divisor + chevron); menu blur 015 intacto
- Sair: hangup + «Sair» com `.btn-danger` (tokens claro/escuro)

**Validação**
- `npx tsc --noEmit` OK
- Quickstart browser §1–§4: não executado aqui (sem browser nesta sessão) — recarregar a chamada no `npm run dev` para rever

**Artefactos**
- Spec/plan/tasks: `specs/020-call-control-icons/`
- CHANGELOG: `[Unreleased]` Changed

### [021-message-hover-highlight](../../specs/021-message-hover-highlight/)

Concluído `/speckit-implement` (T001–T009).

**Entrega**
- `.msg-block` com padding/raio para o fundo cobrir texto + anexos
- `:hover` / `:focus-within` usam `var(--hover)`; avatar/nome ficam fora do bloco (sem destaque)
- `.msg-highlight` (017) continua mais marcado (accent + anel inset); `prefers-reduced-motion` sem transição
- CSS-only; `Channel.tsx` e backend intocados nesta feature

**Validação**
- `node_modules/.bin/tsc --noEmit` OK
- Quickstart browser §1–§5: não executado aqui (sem ferramentas de browser nesta sessão) — recarregar o canal de texto no `npm run dev` para rever hover/foco/salto

**Artefactos**
- Spec/plan/tasks: `specs/021-message-hover-highlight/`
- CHANGELOG: `[Unreleased]` Added

### [023-fix-blur-menu](../../specs/023-fix-blur-menu/)

Concluído `/speckit-implement` (T001–T011).

**Entrega**
- `.call-ctrl-split`: `overflow: visible` (deixava de clipar `.camera-blur-menu`); cantos em pílula nos botões extremos
- `z-index` do split/menu reforçado para modo palco
- Sem Portal; lógica 015 (toggle/Escape/fora) intacta

**Validação**
- `npx tsc --noEmit` OK
- Quickstart browser §1–§3: não executado aqui — clicar na seta da câmara em chamada (e modo palco) no `npm run dev`

**Artefactos**
- Spec/plan/tasks: `specs/023-fix-blur-menu/`
- CHANGELOG: `[Unreleased]` Fixed

### [022-search-group-highlight](../../specs/022-search-group-highlight/)

Concluído `/speckit-implement` (T001–T011).

**Entrega**
- Após salto da pesquisa, `msg-highlight` aplica-se a `.msg-group` (avatar + meta + todas as bolhas), não a `.msg-block`
- Sem ênfase extra na bolha do hit; `scrollIntoView` continua na mensagem; timer ~3 s / substituição / toast 017 intactos
- CSS: `.msg-group.msg-highlight` (accent + anel); removido `.msg-block.msg-highlight`; hover 021 inalterado

**Validação**
- `node_modules/.bin/tsc --noEmit` OK
- Quickstart browser §1–§4: não executado aqui (sem ferramentas de browser nesta sessão) — pesquisar um hit num grupo com várias mensagens no `npm run dev`

**Artefactos**
- Spec/plan/tasks: `specs/022-search-group-highlight/`
- CHANGELOG: `[Unreleased]` Changed

### [025-ws-disconnect-proxy](../../specs/025-ws-disconnect-proxy/)

Concluído `/speckit-implement` (T001–T017).

**Entrega**
- Leave LiveKit ordenado: unpublish cam/mic → `room.disconnect(true)` → terminate E2EE worker (`liveClient.ts`)
- Deduplicar leave/`onCleanup`; `intentionalLeave` não mostra «Ligação encerrada» no Sair
- Vite `customLogger`: filtra só `ws proxy error` + `This socket has been ended by the other party` (half-close `/rtc` após close ordenado); outros erros de proxy mantêm-se
- Nota em `docs/operar-instancia.md`

**Validação**
- `npx tsc --noEmit` OK
- Baseline do spam confirmada no terminal Vite pré-fix
- Quickstart §2–§4: reiniciar `npm run dev` (logger) e validar 5 leaves / rejoin / falha forçada manualmente

**Artefactos**
- Spec/plan/tasks: `specs/025-ws-disconnect-proxy/`
- CHANGELOG: `[Unreleased]` Fixed

## Outro

- Processo: a partir de hoje, cada `/speckit-implement` concluído actualiza `docs/daily/yyyy-mm-dd.md` e o `CHANGELOG.md`.
