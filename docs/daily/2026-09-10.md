# Daily — 2026-09-10

## Speckit implement

### [083-grade-screen-tiles](../../specs/083-grade-screen-tiles/)

- Grade screen share no longer uses split screen/camera bands; cameras and screens share one equal CSS grid (cams first, then screens).
- Screen tiles: handle + share chip (`IconScreenShare` + i18n); spotlight only on screen tiles (span/emphasis in the unified grid); attach by `cam:` / `screen:` keys.
- Capture, Composition (no screen video), occupancy indicators, and Grade-only share control unchanged from 082.
- Validated: `frontend` `tsc --noEmit` OK; quickstart 1–5 reviewed against contracts.
- Specs: [spec](../../specs/083-grade-screen-tiles/spec.md), [plan](../../specs/083-grade-screen-tiles/plan.md), [tasks](../../specs/083-grade-screen-tiles/tasks.md).

### [084-user-panel-stack](../../specs/084-user-panel-stack/)

- User panel stacks call controls on a **centered upper row** when a single row would leave the handle below `--user-panel-name-min` (6ch); Settings stays on the identity row.
- Taller panel **reflows** `.shell-nav` (channel list cedes space; no overlay). Hysteresis via `--user-panel-name-unstack` (7.5ch).
- Touched: `frontend/src/shell/UserPanel.tsx`, `frontend/src/styles/mesa-theme.css`.
- Validated: `frontend` `tsc --noEmit` OK; quickstart Q1–Q5 / UPS-01…09 ready for manual pass.
- Specs: [spec](../../specs/084-user-panel-stack/spec.md), [plan](../../specs/084-user-panel-stack/plan.md), [tasks](../../specs/084-user-panel-stack/tasks.md), [contracts](../../specs/084-user-panel-stack/contracts/user-panel-stack.md).

### [085-screen-share-fit](../../specs/085-screen-share-fit/)

- Grade screen-share tiles use `object-fit: contain` + dark letterbox (no webcam cover crop); cameras unchanged; no mirror on screen video.
- CSS only under `.grade-screen-tile` in `mesa-theme.css`; local preview inherits via `screen:` attach path; spotlight keeps contain.
- Validated: `frontend` `tsc --noEmit` OK; quickstart 1–5 reviewed against SF-01…07.
- Specs: [spec](../../specs/085-screen-share-fit/spec.md), [plan](../../specs/085-screen-share-fit/plan.md), [tasks](../../specs/085-screen-share-fit/tasks.md).

### [086-fix-spotlight-layout](../../specs/086-fix-spotlight-layout/)

- Grade screen spotlight no longer uses grid `span 2` (crushed ~8px tile); spotlight-on = **main stage** (highlighted screen) + **bottom filmstrip** (cams / other screens); off = unified 083 grid.
- Touched: `CameraGrid.tsx` (`splitSpotlightTiles`), `mesa-theme.css` (`.grade-stage-spotlight` / main / strip). VoiceChannel clear-on-stop unchanged.
- Validated: `frontend` `tsc --noEmit` OK; quickstart 1–5 / SL-01…09 reviewed.
- Specs: [spec](../../specs/086-fix-spotlight-layout/spec.md), [plan](../../specs/086-fix-spotlight-layout/plan.md), [tasks](../../specs/086-fix-spotlight-layout/tasks.md).

### [087-disconnect-on-unload](../../specs/087-disconnect-on-unload/)

- Unload (refresh/tab close): `leaveVoiceKeepalive` (`fetch` + `keepalive`) before hangup so occupancy leave can complete during teardown.
- Server: `expire_stale` public + 20s sweeper in `main` broadcasts stale occupant removal (TTL 45s); contract test `stale_occupant_cleared_by_expire_stale_direct`.
- Validated: `cargo test --test contract stale_occupant` OK; `tsc --noEmit` OK.
- Specs: [spec](../../specs/087-disconnect-on-unload/spec.md), [plan](../../specs/087-disconnect-on-unload/plan.md), [tasks](../../specs/087-disconnect-on-unload/tasks.md).

### [088-clear-ended-screen-tile](../../specs/088-clear-ended-screen-tile/)

- Ending screen share clears Grade screen tiles (no empty «Tela» cell): `TrackUnsubscribed` + participant disconnect wipe `remotesScreen`; local `stopScreenShare` still patches `screen_on: false`.
- Spotlight drops ended screen identity; sidebar/channel share indicators follow occupancy as before.
- Touched: `liveClient.ts`, `VoiceSession.tsx`, `VoiceChannel.tsx`.
- Validated: `frontend` `tsc --noEmit` OK; quickstart 1–4 ready for manual two-browser pass.
- Specs: [spec](../../specs/088-clear-ended-screen-tile/spec.md), [plan](../../specs/088-clear-ended-screen-tile/plan.md), [tasks](../../specs/088-clear-ended-screen-tile/tasks.md).

### [089-mesa-a-vela-reskin](../../specs/089-mesa-a-vela-reskin/)

- Visual identity **Mesa à Vela**: amber/jade parchment tokens in Nocturne + Mesa; self-hosted Manrope (UI) + Fraunces (place) with Inter fallback; `--font-place` + `--radius-token` for seats.
- Voice header: Editar cena + blur moved into `⋯` overflow; E2EE chip uses jade; camera seat tones + amber speaking (static under reduced motion).
- Chat pill composer + circular send; Auth/sidebar place typography; screen tiles keep 085 contain.
- Validated: `frontend` `tsc --noEmit` OK; contrast checklist started; manual quickstart §1–§6 for reviewer.
- Specs: [spec](../../specs/089-mesa-a-vela-reskin/spec.md), [plan](../../specs/089-mesa-a-vela-reskin/plan.md), [tasks](../../specs/089-mesa-a-vela-reskin/tasks.md).

### [090-revert-mesa-vela](../../specs/090-revert-mesa-vela/)

- Full product rollback of 089: blurple/Inter tokens; Editar cena + blur in default voice header; seats/place/composer/jade chip removed; Manrope/Fraunces fonts deleted.
- Preserved 082–088 (screen share, Grade, spotlight, user panel, unload leave, clear screen tile).
- Validated: `frontend` `tsc --noEmit` OK; hard-refresh recommended for CSS/fonts.
- Specs: [spec](../../specs/090-revert-mesa-vela/spec.md), [plan](../../specs/090-revert-mesa-vela/plan.md), [tasks](../../specs/090-revert-mesa-vela/tasks.md).

### [091-fix-webcam-attach](../../specs/091-fix-webcam-attach/)

- Fix blank webcam in Composition/Grade after 082 layout forking: session `localVideoEl` is canonical; mid-call `toggleCam` calls `onLocalTrack` (join-equivalent); `layoutMedia` reads session preview + rebinds on host mount (rAF/microtask) and prunes stale tile/slot maps.
- Share start no longer required to “wake” cams; PiP unchanged (out of scope).
- Touched: `VoiceSession.tsx`, `VoiceChannel.tsx`.
- Validated: `frontend` `tsc --noEmit` OK; manual quickstart §1–§7 for reviewer (local ≤2s, remount ≤1s).
- Specs: [spec](../../specs/091-fix-webcam-attach/spec.md), [plan](../../specs/091-fix-webcam-attach/plan.md), [tasks](../../specs/091-fix-webcam-attach/tasks.md).
