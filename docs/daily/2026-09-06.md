# Daily — 2026-09-06

## Speckit implement

### [044-rounded-borders](../../specs/044-rounded-borders/)

Concluído `/speckit-implement` (T001–T019).

**Entrega**
- Tokens `--radius-sm|md|lg` → **8 / 14 / 22px** em `nocturne.css`; pill 999 intacto
- Hardcodes de caixa em `mesa-theme.css` → `var(--radius-*)`; assimétrico `0 4px…` → sm; fallback `10px` → `14px`
- Círculos `50%` e pílulas `999px` preservados; sem edits LiveKit/vendor
- Inline `SceneEditor` preview `2px` deixado (mini-esquema, fora do chrome Mesa)

**Validação**
- `cd frontend && ./node_modules/.bin/tsc --noEmit` OK
- Grep residual `6|8|10px` em `mesa-theme.css`: zero
- Manual [quickstart.md](../../specs/044-rounded-borders/quickstart.md) (HMR / tema claro+escuro)

**Artefactos**
- Spec/plan/tasks: `specs/044-rounded-borders/`
- Contrato: [radius-scale.md](../../specs/044-rounded-borders/contracts/radius-scale.md)
- CHANGELOG: `[Unreleased]` Changed

### [043-panel-calls-in-call-only](../../specs/043-panel-calls-in-call-only/)

Concluído `/speckit-implement` (T001–T013) — **verify-first**, sem regressão de código.

**Entrega**
- Confirmado: `showCallGroup = live && !onStage` + `<Show>`; sem path `is-disabled` no painel
- Identidade sempre montada; palco mantém `call-controls` quando `live && !editing`
- Comentário de predicado actualizado (042/043)

**Validação**
- `cd frontend && ./node_modules/.bin/tsc --noEmit` OK
- Source/grep + contrato [panel-calls-visibility.md](../../specs/043-panel-calls-in-call-only/contracts/panel-calls-visibility.md); quickstart A–C: hard refresh se DOM antigo ainda mostrar `is-disabled`

**Artefactos**
- Spec/plan/tasks: `specs/043-panel-calls-in-call-only/`
- CHANGELOG: `[Unreleased]` Changed

### [042-panel-call-stage-ui](../../specs/042-panel-call-stage-ui/)

Concluído `/speckit-implement` (T001–T015).

**Entrega**
- UserPanel: grupo de chamada só com `live && !onStage` (sem chrome disabled)
- Ícones mic/deafen/cam/sair com `PANEL_CALL_ICON` (18) + CSS svg box
- `.stage` margem/padding + `.voice-pane .pane-header` + `.privacy-line` compactos (~40–80px)

**Validação**
- `cd frontend && ./node_modules/.bin/tsc --noEmit` OK
- Manual [quickstart.md](../../specs/042-panel-call-stage-ui/quickstart.md) (HMR)

**Artefactos**
- Spec/plan/tasks: `specs/042-panel-call-stage-ui/`
- CHANGELOG: `[Unreleased]` Changed

### [041-server-scoped-pane](../../specs/041-server-scoped-pane/)

Concluído `/speckit-implement` (T001–T019) + Convergence (T020–T023).

**Entrega**
- `selectServerAndNavigate`: rail → resolve canal (último local → texto → qualquer) ou `/servers/:id` vazio
- Preferência `mesa.lastChannelByServer`; escrita em ChannelRoute + clique no sidebar
- `EmptyServerPane` + piadas PT-BR; guard mismatch `?server=` vs `channel.server_id`
- Sem hangup ao trocar de servidor (FR-008)
- **US4**: `selectedServerId` inicia a partir da URL no remount do AppShell; rail usa `selectedServerId` (não fallback a `servers[0]`); Sidebar não força `list[0]` quando a URL já identifica o servidor; `.active` do canal só se o id está na lista do servidor actual

**Validação**
- `cd frontend && ./node_modules/.bin/tsc --noEmit` OK
- Manual [quickstart.md](../../specs/041-server-scoped-pane/quickstart.md) + US4 (rail = servidor do painel vazio)

**Artefactos**
- Spec/plan/tasks: `specs/041-server-scoped-pane/`
- CHANGELOG: `[Unreleased]` Changed / Fixed

### [040-remove-connected-bar](../../specs/040-remove-connected-bar/)

Concluído `/speckit-implement` (T001–T019).

**Entrega**
- Removida `voice-connected-bar` do `AppShell`; predicado `showVoicePip` só monta o PiP
- PiP: header = título/timer (drag); rodapé = «Voltar à mesa» + ícone vermelho hangup (`voice.hangup()`, sem navigate)
- CSS: `.voice-pip-actions` / `.voice-pip-timer`; regras da barra removidas
- Coordenação 039: UserPanel continua a oferecer leave off-stage; PiP hangup por conveniência

**Validação**
- `cd frontend && ./node_modules/.bin/tsc --noEmit` OK
- Manual [quickstart.md](../../specs/040-remove-connected-bar/quickstart.md) (HMR)

**Artefactos**
- Spec/plan/tasks: `specs/040-remove-connected-bar/`
- CHANGELOG: `[Unreleased]` Changed

### [039-floating-user-bar](../../specs/039-floating-user-bar/)

Concluído `/speckit-implement` (T001–T025).

**Entrega**
- `UserPanel` no fundo da sidebar: identidade + menu de conta; TopBar sem chip de conta
- Controlos de chamada num sítio activo: palco vs painel (`viewingActiveVoiceStage`); ocultos no painel no palco; disabled fora de chamada
- Deafen cliente (`VoiceSession`: silenciar remotes + mutar mic; unmute limpa deafen); `toggleMic` / `toggleCam` / `toggleDeafen` partilhados
- Câmara + blur + sair no painel off-stage; hangup canónico off-stage = painel (coordenar com 040 PiP)

**Validação**
- `cd frontend && ./node_modules/.bin/tsc --noEmit` OK
- Manual [quickstart.md](../../specs/039-floating-user-bar/quickstart.md) (HMR)

**Artefactos**
- Spec/plan/tasks: `specs/039-floating-user-bar/`
- CHANGELOG: `[Unreleased]` Added

### [037-discord-visual-alignment](../../specs/037-discord-visual-alignment/)

Concluído `/speckit-implement` (T001–T029).

**Entrega**
- Tipografia Inter self-hosted (`frontend/public/fonts/`) + tokens `--font-*`
- Glifos filled nos call-controls (mic/câmera/sair); chrome intacto
- `channel_read_state` + `PUT /api/channels/{id}/read`; `has_unread` / `has_voice` em `GET /api/servers`
- Rail: pill unread + ponto de voz coexistentes; morph círculo→squircle
- `--shadow-float` tema-aware; animação `mesa-float-enter`; reduced-motion

**Validação**
- `cargo test --test contract channel_read_state` OK (2)
- `frontend/.../tsc --noEmit` OK
- Quickstart visual: HMR / reiniciar backend para migração 0010

**Artefactos**
- Spec/plan/tasks: `specs/037-discord-visual-alignment/`
- CHANGELOG: `[Unreleased]` Added

### [038-floating-voice-pip](../../specs/038-floating-voice-pip/)

Concluído `/speckit-implement` (T001–T021).

**Entrega**
- Miniatura flutuante da chamada (`FloatingVoicePip`) ao navegar para texto (mesmo predicado da barra ligada)
- Preview LiveKit (até 4 cams) + fallback «Em chamada»; drag com snap aos 4 cantos; canto em memória até Sair (sem localStorage)
- «Voltar à mesa» no PiP; barra coexistente com Sair

**Validação**
- `cd frontend && npx tsc --noEmit` OK
- Manual [quickstart.md](../../specs/038-floating-voice-pip/quickstart.md) (HMR no Vite)

**Artefactos**
- Spec/plan/tasks: `specs/038-floating-voice-pip/`
- CHANGELOG: `[Unreleased]` Added

### [036-mic-ctrl-speaking-aura](../../specs/036-mic-ctrl-speaking-aura/)


Concluído `/speckit-implement` (T001–T012). Reutiliza `VoiceSession.speakingAccountIds` (033).

**Entrega**
- Aura `is-speaking` no **botão inteiro** de microfone em `.call-controls` (`VoiceChannel`)
- Gate: `micOn && speakingAccountIds.has(me.id)`; rótulos «Microfone ligado/desligado» intactos
- CSS partilha `@keyframes voice-roster-speak-aura` + overrides tema claro; sem aura em cam/blur/Sair

**Validação**
- `frontend/node_modules/typescript/bin/tsc --noEmit` OK
- Quickstart US1–US3: HMR no Vite; verificar falando/mute na mesa

**Artefactos**
- Spec/plan/tasks: `specs/036-mic-ctrl-speaking-aura/`
- CHANGELOG: `[Unreleased]` Added

### [031-voice-join-errors](../../specs/031-voice-join-errors/)

- Entregue: abort de join falhado (`abortFailedJoin`) com `leaveVoice` + stop de tracks/blur; mensagens PT por categoria (`joinErrors`); fallback áudio-only quando a câmera falha; `liveClient` deixa de auto-activar câmera sem track.
- Validação: `cd frontend && npx tsc --noEmit` OK; manual [quickstart.md](../../specs/031-voice-join-errors/quickstart.md) (indicador de captura / ghost occupancy).
- Specs: [spec](../../specs/031-voice-join-errors/spec.md), [plan](../../specs/031-voice-join-errors/plan.md), [tasks](../../specs/031-voice-join-errors/tasks.md).

### [032-voice-join-camera-choice](../../specs/032-voice-join-camera-choice/)

- Entregue: pré-join com «Entrar com câmera» / «Entrar sem câmera (banco)»; join `cam_on: false` sem auto-slot; PATCH `cam_on: true` auto-slot só em cena auto + slot livre; toggle cam após áudio-only; move de canal volta ao pré-join.
- Validação: `cargo test --test contract` (78 ok); `cd frontend && npx tsc --noEmit` OK.
- Specs: [spec](../../specs/032-voice-join-camera-choice/spec.md), [plan](../../specs/032-voice-join-camera-choice/plan.md), [tasks](../../specs/032-voice-join-camera-choice/tasks.md).

### [033-voice-speaking-indicator](../../specs/033-voice-speaking-indicator/)

- Entregue: ícones mic + headphones na lista aninhada; aura `is-speaking` via LiveKit `ActiveSpeakersChanged` (só se viewer `live` na mesma mesa); mute bloqueia aura; debounce 120 ms.
- Validação: `cd frontend && npx tsc --noEmit` OK; manual [quickstart.md](../../specs/033-voice-speaking-indicator/quickstart.md).
- Specs: [spec](../../specs/033-voice-speaking-indicator/spec.md), [plan](../../specs/033-voice-speaking-indicator/plan.md), [tasks](../../specs/033-voice-speaking-indicator/tasks.md).

### [034-scene-editor-side-layout](../../specs/034-scene-editor-side-layout/)

- Entregue: painel lateral do editor com 3 blocos `flex: none` (slots compactos no topo); deixou de reutilizar `.sidebar-section { flex: 1 }` que esticava os rótulos.
- Validação: `cd frontend && npx tsc --noEmit` OK; manual [quickstart.md](../../specs/034-scene-editor-side-layout/quickstart.md) (SC-001 altura slots).
- Specs: [spec](../../specs/034-scene-editor-side-layout/spec.md), [plan](../../specs/034-scene-editor-side-layout/plan.md), [tasks](../../specs/034-scene-editor-side-layout/tasks.md).

### [035-voice-leave-release-media](../../specs/035-voice-leave-release-media/)

- Entregue: `releaseLocalCapture` (blur + track.stop + LiveKit disable/disconnect); hangup/dropped/disconnectLivekitOnly/pagehide e `abortFailedJoin` usam o mesmo caminho; leave local antes de `leaveVoice`.
- Validação: `cd frontend && npx tsc --noEmit` OK; manual [quickstart.md](../../specs/035-voice-leave-release-media/quickstart.md) (indicador de captura).
- Specs: [spec](../../specs/035-voice-leave-release-media/spec.md), [plan](../../specs/035-voice-leave-release-media/plan.md), [tasks](../../specs/035-voice-leave-release-media/tasks.md).
