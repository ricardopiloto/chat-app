# Daily — 2026-09-08

## Speckit implement

### [081-panel-cam-header-blur](../specs/081-panel-cam-header-blur/)

- Camera on user panel (after deafen, before settings); preferred `camOn` session default off; JOIN uses panel pref; pre-join stripped to JOIN (+ test).
- Header blur `<select>` (none/light/strong) while live; removed `voice-in-call-bar`; stage mode retired (always off).
- Touch: `UserPanel.tsx`, `VoiceSession.tsx`, `VoiceChannel.tsx`, `AppShell.tsx`, `uiPrefs.ts`, i18n, `mesa-theme.css`.
- Validation: `tsc --noEmit` OK. Quickstart A–E for manual.
- Specs: [spec](../specs/081-panel-cam-header-blur/spec.md), [tasks](../specs/081-panel-cam-header-blur/tasks.md) T001–T019, [contracts](../specs/081-panel-cam-header-blur/contracts/).

### [080-call-controls-restore](../specs/080-call-controls-restore/)

- Leave always on user panel while live (including stage); mic/deafen operable out of call as session prefs; join inherits mic/deafen (`VoiceSession` + `VoiceChannel.connect`).
- Restored voice-pane **bottom bar**: hang-up, mid-call camera (`toggleCam`), blur only when camera on; blur menu placement/z-index vs pane header.
- Panel polish: no decorative mic/deafen chevrons; soft rounded controls (not pill).
- Touch: `UserPanel.tsx`, `VoiceSession.tsx`, `VoiceChannel.tsx`, `CameraBlurMenu.tsx`, `mesa-theme.css`.
- Validation: `tsc --noEmit` OK. Quickstart A–G for manual.
- Specs: [spec](../specs/080-call-controls-restore/spec.md), [tasks](../specs/080-call-controls-restore/tasks.md) T001–T019, [contracts](../specs/080-call-controls-restore/contracts/).

### [079-chat-image-zoom](../specs/079-chat-image-zoom/)

- FE lightbox over existing decrypted attachment blobs (`MessageAttachments` → `ImageLightbox`): fit on open, zoom/pan, Escape/backdrop/X, same-message prev/next (no wrap + zoom reset), download.
- i18n EN/pt-BR; CSS `.img-lightbox*` + clickable `.msg-attach-open`.
- Validation: `tsc --noEmit` OK. Quickstart A–F for manual.
- Specs: [spec](../specs/079-chat-image-zoom/spec.md), [tasks](../specs/079-chat-image-zoom/tasks.md) T001–T020, [contracts/image-lightbox-ui.md](../specs/079-chat-image-zoom/contracts/image-lightbox-ui.md).

### [078-user-panel-discord](../specs/078-user-panel-discord/)

- Channel-list **collapse deferred**: always expanded; peek/toggle removed; legacy pref ignored (`AppShell` / `Sidebar` / `uiPrefs`).
- **User panel** Discord layout: handle + Online; always mic▾ / deafen▾ / settings; leave left of trio when live off-stage; no camera/blur on panel.
- Voice **pre-join**: single JOIN (default camera off); opt-in + lazy preview; blur gated on camera intent; 032 banco/palco via `connect(audio|camera)`.
- Validation: `tsc --noEmit` OK. Quickstart A–E for manual smoke.
- Specs: [spec](../specs/078-user-panel-discord/spec.md), [tasks](../specs/078-user-panel-discord/tasks.md) T001–T024, [contracts](../specs/078-user-panel-discord/contracts/).

### [077-member-join-welcome](../specs/077-member-join-welcome/)

- System welcome on invite join via `emit_invite_consumed` → `announce_member_join` (non-fatal); destination owner → `geral` → invite `welcome_channel_id`; template `{nome}` / default PT copy.
- Migration `0019` (pre-existing): `message.kind` + `content_plaintext`; server/invite welcome fields. System rows use empty ciphertext; FK sender = joining member, API omits sender for `kind=system`.
- Owner GET/PATCH `/api/servers/{id}/welcome`; create-invite requires channel when no owner dest and no `geral`.
- FE: centered `.msg-system` timeline items; Sidebar welcome-channel picker; settings page + nav; i18n EN/pt-BR.
- Validation: `cargo test --test contract member_join_welcome` 6 OK; `tsc --noEmit` OK. Quickstart A–E left for manual (T025).
- Specs: [spec](../specs/077-member-join-welcome/spec.md), [tasks](../specs/077-member-join-welcome/tasks.md) T001–T024/T026–T027, [contracts](../specs/077-member-join-welcome/contracts/).

### [076-app-version-label](../specs/076-app-version-label/)

- Product version `0.5.0` aligned in `frontend/package.json` + `backend/Cargo.toml`; Vite `__APP_VERSION__` → `lib/appVersion.ts`.
- Public AuthShell: version in `.auth-brand-footer` (not under hero name); TopBar: version under «Mesa» in brand text column.
- Touch: `vite.config.ts`, `appVersion.ts`, `AuthShell.tsx`, `TopBar.tsx`, `mesa-theme.css`, `vite-env.d.ts`.
- Validation: `tsc --noEmit` OK; quickstart A–C for manual.
- Specs: [spec](../specs/076-app-version-label/spec.md), [tasks](../specs/076-app-version-label/tasks.md) T001–T015.

### [075-panel-cam-ctrl-align](../specs/075-panel-cam-ctrl-align/)

- User-panel camera/blur split: panel-scoped CSS overrides global `.call-ctrl-split` (44×48) → **32px** height matching mic/deafen/leave; horizontal compact chevron kept; blur menu unchanged.
- Stage + channels collapsed: call controls **centered**; leave `margin-left: auto` neutralized only in that state; tighter gap/chevron for fit / no closed overflow.
- Touch: `mesa-theme.css` only. Validation: CSS-only (no TS change); quickstart A–D for manual smoke.
- Specs: [spec](../specs/075-panel-cam-ctrl-align/spec.md), [plan](../specs/075-panel-cam-ctrl-align/plan.md), [tasks](../specs/075-panel-cam-ctrl-align/tasks.md) T001–T017.

### [074-i18n-en-ptbr](../specs/074-i18n-en-ptbr/)

- Custom FE i18n (`frontend/src/i18n/`): catalogs **pt-BR** + **en**, `detectLocale`, `mesa.locale` via `localPrefs`, Solid-reactive `t()` / `setLocale`.
- Auth: browser/system auto-detect (persist on first visit); no language picker on auth.
- Authenticated: language control in **AccountMenu** (exactly EN + pt-BR); instant switch without logout.
- Product chrome migrated (shell, channels, voice, settings, dialogs, day/notif labels); user content (messages, channel/server names, custom roles, handles) left raw; system role **Dono** → `systemRoleLabel`.
- Validation: `npx tsc --noEmit` OK; catalogs key trees in sync (~341 keys). Manual quickstart A–F recommended.
- Specs: [spec](../specs/074-i18n-en-ptbr/spec.md), [plan](../specs/074-i18n-en-ptbr/plan.md), [tasks](../specs/074-i18n-en-ptbr/tasks.md) T001–T027. Milestone capability for **0.6.0** (no version bump/tag in this implement).

### [073-role-card-name-only](../specs/073-role-card-name-only/)

- Roles manage `.permission-card-heading`: only role **name** (removed «posição N» and «(sistema)»); ↑/↓ / Permissões / Apagar unchanged.
- Touch: `RolesManagePage.tsx`. Validation: `tsc --noEmit` OK.
- Specs: [spec](../specs/073-role-card-name-only/spec.md), [tasks](../specs/073-role-card-name-only/tasks.md) T001–T009.

### [072-channel-name-lock](../specs/072-channel-name-lock/)

- Channel name: live spaces→`-`, max 32 Unicode scalars; reject empty / hyphen-only; create+rename + BE `channel_name` validate.
- Private sidebar lock aligned **right**; **clear reserve** under lock (padding 36px) with fade only in reserved band; create/rename inputs capped so **no horizontal scroll** of sidebar/row (caret may scroll inside input).
- Touch: `channelName.ts`, `Sidebar.tsx`, `mesa-theme.css`, `domain/channel_name.rs`, `channel_provision`/`channels` PATCH, contract `channel_name_rules`.
- Validation: `tsc --noEmit` OK; `cargo test channel_name_rules` + unit tests OK; convergence T018–T021 polish.
- Specs: [spec](../specs/072-channel-name-lock/spec.md), [tasks](../specs/072-channel-name-lock/tasks.md) T001–T021.

### [071-notif-seen-collapse](../specs/071-notif-seen-collapse/)

- Durable Menção/Resposta clear when message enters history viewport (not on channel enter); `mesa:notification-message-read` syncs TopBar.
- Session unseen: per-message store; ≤5 detail rows / channel, else single **5+** → oldest `?msg=`; mentions/replies never collapse.
- **Limpar**: `POST /api/notifications/read-all` + clear session; bell icon 24px; badge stays binary dot.
- Touch: `notifications.ts`, `notifSync.ts`, `App.tsx`, `ChannelRoute.tsx`, `Channel.tsx`, `TopBar.tsx`, `client.ts`, BE `notifications` + contract test, `mesa-theme.css`.
- Validation: `tsc --noEmit` OK; `cargo test mark_all_notifications_read` OK; quickstart A–E for manual.
- Specs: [spec](../specs/071-notif-seen-collapse/spec.md), [tasks](../specs/071-notif-seen-collapse/tasks.md) T001–T024, [contracts](../specs/071-notif-seen-collapse/contracts/).

### [070-emoji-hypertext-picker](../specs/070-emoji-hypertext-picker/)

- Unicode emoji in channel names (create/rename + picker) and message body; static shortcode catalog.
- Composer chrome: `+` left inside field; emoji + paper-plane send right; text padding avoids icon overlap; send disabled when empty.
- `:shortcode` filtered suggest (Enter/click); no auto-replace; mutually exclusive with `@` mentions.
- Touch: `emojiData.ts`, `emojiShortcode.ts`, `EmojiPicker`/`EmojiSuggest`, `IconSend`/`IconEmoji`, `Channel.tsx`, `Sidebar.tsx`, `mesa-theme.css`.
- Validation: `tsc --noEmit` OK; quickstart A–G ready for manual.
- Specs: [spec](../specs/070-emoji-hypertext-picker/spec.md), [tasks](../specs/070-emoji-hypertext-picker/tasks.md) T001–T022, [contracts](../specs/070-emoji-hypertext-picker/contracts/).

### [069-idle-chat-stall](../specs/069-idle-chat-stall/)

- Fix silent stall after idle: `connectLiveWs` reconnect + backoff + ping; Channel incremental catch-up (merge by id); delivery banner while degraded.
- Touch: `ws.ts`, `App.tsx`, `ChannelRoute.tsx`, `Channel.tsx`, `messageCatchUp.ts`, `mesa-theme.css`. Post-F5 unlock unchanged.
- Validation: `tsc --noEmit` OK; quickstart A–F ready for manual.
- Specs: [spec](../specs/069-idle-chat-stall/spec.md), [tasks](../specs/069-idle-chat-stall/tasks.md) T001–T016, [contracts](../specs/069-idle-chat-stall/contracts/).

### [068-notif-channel-datetime](../specs/068-notif-channel-datetime/)

- TopBar notification list: durable rows = **channel name** + local when (`Hoje`/`Ontem`/`DD mmm` + `HH:MM`); session unseen = name only; no Menção/Resposta or UUID slice.
- Resolve names via cached `GET /api/channels/{id}` (dedupe); fallback «Canal indisponível».
- Touch: `notifFormat.ts`, `TopBar.tsx`, `mesa-theme.css` (`.topbar-notif-when`). Click / mark-read / `?msg=` unchanged (062).
- Validation: `tsc --noEmit` OK; quickstart A–F ready for manual.
- Specs: [spec](../specs/068-notif-channel-datetime/spec.md), [tasks](../specs/068-notif-channel-datetime/tasks.md) T001–T015, [contracts](../specs/068-notif-channel-datetime/contracts/).

### [067-mention-handle-style](../specs/067-mention-handle-style/)

- Message body `@handle` chips: background + bold for **other** people (pattern tokenize); self stays plain; emails not styled.
- Click styled mention → open members panel focused on that account when in roster; unavailable = styled no-op.
- Touch: `mentionParse.ts` (`tokenizeMentionsForDisplay`), `MessageBody.tsx`, `Channel.tsx`, `AppShell`/`MembersPanel` focus, `mesa-theme.css`.
- Validation: `tsc --noEmit` OK; quickstart A–F ready for manual.
- Specs: [spec](../specs/067-mention-handle-style/spec.md), [tasks](../specs/067-mention-handle-style/tasks.md) T001–T016, [contracts](../specs/067-mention-handle-style/contracts/).

### [066-topbar-notif-panel](../specs/066-topbar-notif-panel/)

- TopBar notificações panel no longer clipped: `.topbar` `overflow: visible` + `z-index: 50` so dropdown paints above `.shell`.
- Panel `max-height` + `overflow-y: auto` for long lists / narrow viewports; `Show when={notifOpen()}` unchanged (no ghost).
- Touch: `frontend/src/styles/mesa-theme.css` only (no TopBar.tsx / API).
- Validation: `tsc --noEmit` OK; quickstart A–C ready for manual.
- Specs: [spec](../specs/066-topbar-notif-panel/spec.md), [tasks](../specs/066-topbar-notif-panel/tasks.md) T001–T012, [contracts](../specs/066-topbar-notif-panel/contracts/).

### [065-mention-autocomplete](../specs/065-mention-autocomplete/)

- Composer `@` picker: channel viewers only (exclude self), filter while typing, click/keyboard insert `@handle`.
- `GET /api/channels/{id}/mentionables`; send resolves via mentionables so manual `@handle` notifies (062 path no longer silent when roster missing).
- Touch: `channels.rs`, `mentionParse.ts`, `MentionPicker.tsx`, `Channel.tsx`, `mesa-theme.css`.
- Validation: `mentionables` + `mentions_replies` contract OK; `tsc --noEmit` OK.
- Specs: [spec](../specs/065-mention-autocomplete/spec.md), [tasks](../specs/065-mention-autocomplete/tasks.md) T001–T019.

### [064-day-sep-full-width](../specs/064-day-sep-full-width/)

- Inline day-sep dash lines span `.text-scroll` **content** width (inside lateral padding): seps moved out of `.text-measure` (74ch); messages stay in measure wrappers.
- Sticky day chip **flush** to scrollport top (padding split: horizontal on `.text-scroll`, top pad on `.text-scroll-body`); pill only, no sticky dash.
- Touch: `Channel.tsx`, `mesa-theme.css`. No day-logic / API changes.
- Validation: `tsc --noEmit` OK; quickstart A–E ready for manual.
- Specs: [spec](../specs/064-day-sep-full-width/spec.md), [tasks](../specs/064-day-sep-full-width/tasks.md) T001–T013, [contracts](../specs/064-day-sep-full-width/contracts/).

### [063-channel-acl-edit-parity](../specs/063-channel-acl-edit-parity/)

- Unified channel manage gate: **owner ∪ creator ∪ («Gerenciar canal» ∧ actor_pos > creator_pos)** for ACL GET/PUT, rename, delete, and access inspect.
- PUT ACL subject hierarchy: actor must outrank account/role subjects (everyone skipped); owner/creator exempt.
- FE: `canManageChannel` drives Permissions / rename / delete menu; API remains source of truth.
- Validation: `cargo test --test contract` **118** OK (incl. `channel_manage_parity`, rename, overwrites); `tsc --noEmit` OK.
- Specs: [spec](../specs/063-channel-acl-edit-parity/spec.md), [tasks](../specs/063-channel-acl-edit-parity/tasks.md) T001–T022, [contracts](../specs/063-channel-acl-edit-parity/contracts/).

### [062-message-mentions-replies](../specs/062-message-mentions-replies/)

- Mentions: client parses `@handle` → `mentioned_account_ids`; server validates viewers, persists `message_mention`, never reads ciphertext.
- Replies: `reply_to_message_id` same-channel; notify parent author; quote + Responder UI; no thread UI.
- Durable notifications (`user_notification`, migration `0018`): `GET /api/notifications`, mark-read, WS `notification.created`; TopBar Menção/Resposta + session unseen; deep-link `?msg=` / «Mensagem indisponível».
- FE: `.msg-highlight-me` until viewport seen (`mesa.highlightSeen`); stick-to-bottom + floating «Saltar para o presente».
- Validation: `cargo test --test contract` **115** OK (incl. `mentions_replies`); `tsc --noEmit` OK.
- Specs: [spec](../specs/062-message-mentions-replies/spec.md), [tasks](../specs/062-message-mentions-replies/tasks.md) T001–T029, [contracts](../specs/062-message-mentions-replies/contracts/).

### [061-channel-day-separators](../specs/061-channel-day-separators/)

- Text channel day separators: inline dash line before each civil day with messages; labels **Hoje** / **Ontem** / `DD Mês AAAA` (local TZ); no lines for empty calendar gaps.
- Sticky day label at scroll top when inline sep scrolled away (hidden while matching inline is at top); midnight/focus refresh for relative labels.
- Touch: `frontend/src/lib/daySeparators.ts`, `Channel.tsx`, `mesa-theme.css`. Frontend only.
- Validation: `tsc --noEmit` OK; quickstart A–F ready for manual.
- Specs: [spec](../specs/061-channel-day-separators/spec.md), [tasks](../specs/061-channel-day-separators/tasks.md) T001–T020, [contracts](../specs/061-channel-day-separators/contracts/).

### [060-permissions-parity-phase1](../specs/060-permissions-parity-phase1/)

- Role **hierarchy** (`server_role.position`): kick/mute/assign/edit/reorder gated; new roles at bottom; Dono pinned top; `PUT …/roles/positions`.
- Channel **Allow/Deny** overwrites + subject **everyone**; resolve base → todos → perfil → membro → dono; Deny view only on private; migration `0017`.
- Denial `code` + PT messages; **inspect** `GET …/channels/{id}/access/{accountId}` + ACL panel UI; RolesManage reorder.
- Validation: `cargo test --test contract` **108** OK (incl. `hierarchy`, `channel_overwrites`); `tsc --noEmit` OK.
- Specs: [spec](../specs/060-permissions-parity-phase1/spec.md), [tasks](../specs/060-permissions-parity-phase1/tasks.md) T001–T031, [contracts](../specs/060-permissions-parity-phase1/contracts/), [gap-analysis](../specs/060-permissions-parity-phase1/gap-analysis.md).

### [059-server-owner-role](../specs/059-server-owner-role/)

- Auto **Dono** system role (`is_system`) on server create + post-migrate backfill; owner assigned; full caps; protected delete/rename/cap edit/assign.
- FE: Perfis hide Apagar; permissions read-only; Gerir membros locks owner to Dono and hides Dono from other pickers.
- Validation: `cargo test --test contract` (incl. `owner_role`) OK; `tsc --noEmit` OK.
- Specs: [spec](../specs/059-server-owner-role/spec.md), [tasks](../specs/059-server-owner-role/tasks.md) T001–T018, [contracts](../specs/059-server-owner-role/contracts/).

### [058-channel-mute-member](../specs/058-channel-mute-member/)

- Cap **Silenciar membros** (`can_mute_members`) + migration `0015` (`channel_mute`); PUT/DELETE/GET mutes API; `post_message` 403 while muted; kick cleans mutes and keeps account.
- Members panel: Silenciar (5/10/15/30 + custom) / Levantar with remaining time when channel selected; Role Permissions toggle; composer banner + disabled send for muted user.
- Validation: `cargo test --test contract` 99 OK (incl. `channel_mute`); `tsc --noEmit` OK.
- Specs: [spec](../specs/058-channel-mute-member/spec.md), [tasks](../specs/058-channel-mute-member/tasks.md) T001–T023, [contracts](../specs/058-channel-mute-member/contracts/).

### [057-system-theme-preference](../specs/057-system-theme-preference/)

- Theme preference `system` | `light` | `dark` in `mesa.theme`; resolve effective via OS when system/absent; legacy light/dark kept.
- Auth/AppShell no longer hardcode dark; early script in `index.html`; TopBar cycles three states (IconSystem / Sun / Moon); `matchMedia` + `storage` listeners.
- Validation: `tsc --noEmit` OK; quickstart A–E ready for manual.
- Specs: [spec](../specs/057-system-theme-preference/spec.md), [tasks](../specs/057-system-theme-preference/tasks.md) T001–T019, [contracts](../specs/057-system-theme-preference/contracts/).

### [056-server-settings-shell](../specs/056-server-settings-shell/)

- Server settings mode via `/servers/:id/settings/*`: gear + name open placeholder; grouped sidebar (Pessoas / Funções / Servidor); TopBar **X** exits to last channel.
- Pages: Membros, Perfis (was modal), Imagem, Apagar; legacy `/members` and role-permissions redirect into settings prefix.
- Removed server-name dropdown and rail context menu Imagem/Apagar; user-panel gear unchanged.
- Validation: `tsc --noEmit` OK.
- Specs: [spec](../specs/056-server-settings-shell/spec.md), [tasks](../specs/056-server-settings-shell/tasks.md) T001–T023, [contracts](../specs/056-server-settings-shell/contracts/).

### [055-channels-rail-drawer](../specs/055-channels-rail-drawer/)

- Desktop channels list collapses behind Server Rail with peek strip (hover enlarge; click/keyboard **opens only**); header «Ocultar / Mostrar canais» outside stage too; reflow open/close (not overlay).
- Pref: `mesa.channelsListExpanded` with read fallback from `mesa.stageChannelsExpanded`; shell class `.channels-collapsed`; mobile hamburger drawer unchanged (FR-009).
- Touch: `uiPrefs.ts`, `AppShell.tsx`, `Sidebar.tsx`, `mesa-theme.css`.
- Validation: `tsc --noEmit` OK; quickstart A–G ready for manual (dev servers already running).
- Specs: [spec](../specs/055-channels-rail-drawer/spec.md), [tasks](../specs/055-channels-rail-drawer/tasks.md) T001–T019, [contracts/channels-rail-drawer](../specs/055-channels-rail-drawer/contracts/channels-rail-drawer.md).

### [054-channel-rename](../specs/054-channel-rename/)

- `PATCH /api/channels/{id}` accepts optional `name` (trim, non-empty; duplicates allowed); auth = owner ∪ creator ∪ `can_manage_channels`; `db::channel::update_name` + `permissions::can_rename_channel`.
- Sidebar: double-click / double-tap inline rename (Enter/Escape/blur); unauthorized users do not enter edit mode; empty/403 restore previous name + error.
- Active channel title updates via `mesa:channel-renamed` in `ChannelRoute`.
- Validation: `cargo test --test contract channel_rename` OK; `tsc --noEmit` OK.
- Specs: [spec](../specs/054-channel-rename/spec.md), [tasks](../specs/054-channel-rename/tasks.md), [contracts/channel-rename-api](../specs/054-channel-rename/contracts/channel-rename-api.md), [contracts/channel-rename-ui](../specs/054-channel-rename/contracts/channel-rename-ui.md).

### [053-frontend-build-optimize](../specs/053-frontend-build-optimize/)

- Lazy-load LiveKit / voice stack via `frontend/src/voice/loadRuntime.ts` + `runtime.ts`; thin `VoiceSession` provider; `VoiceChannel` / PiP / blur load on join or while live.
- Route code-split: `ChannelRoute`, `VoiceChannel`, `Channel`, `FloatingVoicePip`, members/roles pages.
- Loading + **Tentar de novo** UX on voice module failure; Vite `manualChunks` for `livekit` / `livekit-blur` (no `chunkSizeWarningLimit` raise).
- Dedupe: `frontend/src/lib/{apiError,capabilities,localPrefs,panelState,safeMedia,keySyncCopy}.ts`; inventory C1–C9 terminal in `specs/053-…/inventory.md`.
- Validation: `tsc --noEmit` OK; `npm run build` → entry **212.18 kB** (was ~963 kB); livekit async **558.56 kB**, livekit-blur **153.38 kB**.
- Specs: [spec](../specs/053-frontend-build-optimize/spec.md), [tasks](../specs/053-frontend-build-optimize/tasks.md) T001–T033, [contracts](../specs/053-frontend-build-optimize/contracts/).
