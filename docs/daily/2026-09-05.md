# Daily — 2026-09-05

## Speckit implement

### [030-voice-roster-avatars](../../specs/030-voice-roster-avatars/)

Concluído `/speckit-implement` (T001–T016). Sem migração nova: `has_avatar` na vista de ocupação (JOIN `account.avatar_filename`).

**Entrega**
- REST `GET .../voice-occupancy` e WS `voice.occupancy` serializam `has_avatar` por ocupante
- Lista aninhada: `IdentityAvatar` compacto (20px) à esquerda do handle; filtro 028 `mic_on || cam_on` intacto
- Foto partida → iniciais (`onerror`); canal de texto já usava o mapa de membros `has_avatar` (um ícone por grupo)
- Sem WS `avatar.updated`; PUT/DELETE avatar não reemite ocupação

**Validação**
- `cargo test --test contract` — 74 OK (incl. `occupancy_has_avatar_follows_account_photo`)
- `frontend/node_modules/.bin/tsc --noEmit` OK
- Quickstart US1/US4: contratos API; UI no Vite após reiniciar `cargo run` para o campo novo no JSON

**Artefactos**
- Spec/plan/tasks: `specs/030-voice-roster-avatars/`
- CHANGELOG: `[Unreleased]` Added

### [029-user-server-avatars](../../specs/029-user-server-avatars/)

Concluído `/speckit-implement` (T001–T027). Migração **`0009_user_server_avatars.sql`** (`0008` ficou para ocupação de voz).

**Entrega**
- Avatar da conta: `PUT`/`DELETE /api/auth/avatar`, `GET /api/accounts/{id}/avatar`; `has_avatar` em me/login/registo e membros
- Imagem do servidor: `PUT`/`DELETE`/`GET /api/servers/{id}/image` (mutar = dono; GET = membro); `has_image` na listagem
- Ficheiros em claro sob `AVATARS_DIR` (default `./data/avatars`); allowlist JPEG/PNG/WebP, tecto 1 MiB; sem recorte, sem WS
- UI: Foto de perfil no menu da conta; imagem do servidor no menu de dono; chip, membros, mensagens e rail com `object-fit: cover` ou iniciais

**Validação**
- `cargo test --test contract --test integration` — 73 + 2 OK
- `npx tsc --noEmit`
- Quickstart US1–US3: contratos API; UI no Vite após reiniciar `cargo run` para a migração `0009`

**Artefactos**
- Spec/plan/tasks: `specs/029-user-server-avatars/`
- CHANGELOG: `[Unreleased]` Added

### [028-voice-call-roster](../../specs/028-voice-call-roster/)

Concluído `/speckit-implement` (T001–T030).

**Entrega**
- Ocupação de voz no Axum (`voice_occupant`, `channel.voice_session_started_at`); GET snapshot, join upsert/move, leave, PATCH mídia, heartbeat/expiry 45s
- WS `voice.occupancy` (snapshot por canal); leave liberta slot da grade
- Lista aninhada na coluna (só mic **ou** câmara ligados) + cronómetro da sessão da mesa
- Sessão LiveKit no `VoiceSession` (shell): texto não desliga; barra Voltar à mesa / Sair; clicar noutro canal de voz move a mesa
- Painel Membros 008/019 intacto

**Validação**
- `cargo test --test contract --test integration` — 65 + 2 OK
- `npx tsc --noEmit` OK
- Quickstart §1–§6: ocupação/timer/leave/move/stale nos testes de contrato; UI no Vite (`npm run dev`). Reiniciar `cargo run` para a migração `0008` e as rotas novas.

**Artefactos**
- Spec/plan/tasks: `specs/028-voice-call-roster/`
- CHANGELOG: `[Unreleased]` Added / Fixed

### [026-message-delete-icon](../../specs/026-message-delete-icon/)

Concluído `/speckit-implement` (T001–T012).

**Entrega**
- Controlo apagar mensagem: ícone de lixeira (`IconTrash`) em vez do texto «Apagar»
- Fundo + borda + ícone em vermelho claro (`color-mix` com `--color-danger*`), distinto de `.btn-danger` (Sair)
- `title="Apagar"` + `aria-label="Apagar mensagem"`; confirmação e permissões 011 intactos
- Ajuste leve no tema claro; outros «Apagar» (canal/servidor/cena) inalterados

**Validação**
- `npx tsc --noEmit` OK
- Quickstart browser §1–§3: hover na mensagem no `npm run dev` (claro/escuro)

**Artefactos**
- Spec/plan/tasks: `specs/026-message-delete-icon/`
- CHANGELOG: `[Unreleased]` Changed

### [024-security-hardening](../../specs/024-security-hardening/)

Concluído `/speckit-implement` (T001–T026).

**Entrega**
- Perfil de produção recusa chaves LiveKit de exemplo e exige cookie Secure; bind default `127.0.0.1:8080`
- Unfurl com DNS + blocklist de IPs privados, tecto 256 KiB, `image_url` só http(s) no cliente
- Ritmo 10/60s em login/registo; primeiro operador sob `BEGIN IMMEDIATE`
- Join de voz usa só `LIVEKIT_WS_URL`; envelopes sem overwrite de membro `synced`
- CSP / `X-Frame-Options: DENY`; fontes `system-ui` (sem Google Fonts); `authz` partilhado (`require_member`, `history_visible_since`)
- Sem BFF; chaves de canal continuam em `localStorage`

**Validação**
- `cargo test --test contract --test integration` — 57 + 2 OK
- `npx tsc --noEmit` OK
- Quickstart §1–§6 via testes de contrato (boot, unfurl, ritmo, voz, cabeçalhos) e docs LAN/produção

**Artefactos**
- Spec/plan/tasks: `specs/024-security-hardening/`
- CHANGELOG: `[Unreleased]` Added / Changed / Fixed

### [027-auth-login-screen](../../specs/027-auth-login-screen/)

Concluído `/speckit-implement` (T001–T019).

**Entrega**
- `AuthShell` de duas colunas (marca Mesa + tagline + nota self-hosted | formulário), tema escuro forçado no ecrã
- `/auth`: abas Entrar / Criar conta; no login, «ou» + botão outline Criar conta; campos com @ / cadeado e toggle de senha; botão primário roxo
- Desbloquear chaves / outra conta / recuperar identidade no form pane
- `/invite/…` no mesmo chrome; APIs inalteradas; sem «Esqueceu sua senha?»

**Validação**
- `npx tsc --noEmit` OK
- Quickstart browser: abrir `/auth` e comparar com `docs/screenshots/03-auth.jpg`; testar registo, erros, invite

**Artefactos**
- Spec/plan/tasks: `specs/027-auth-login-screen/`
- CHANGELOG: `[Unreleased]` Added
